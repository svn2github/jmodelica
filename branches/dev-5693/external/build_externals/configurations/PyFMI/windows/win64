mkfile_path := $(abspath $(lastword $(MAKEFILE_LIST)))
mkfile_dir := $(dir $(mkfile_path))
include $(mkfile_dir)/win
BITNESS="64"

# Required variables
FMIL_HOME="$(SDK_HOME)/build/FMIL_install64"
# Optional variables
PYTHONHOME=$(SDK_HOME)/Python27/Python_64/
