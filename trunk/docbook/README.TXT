The Makefile is setup for a xsltproc + Apache fop toolchain and has targets for html, pdf, and htmlhelp. 
The 'docbook' target must be built first (once) and will download the required docbook stylesheets.
